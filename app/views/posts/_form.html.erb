<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <div class="notification is-danger">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
      
        <li><%= message %></li>
      <% end %>
      </ul>
      </div>
    </div>
    <BR>
  <% end %>


  <div class="field">
    <%= form.label :calories_earned, class: "label" %>
    <%= form.text_field :calories_earned, id: :post_user_id, class: "input" %>
  </div>
  
  <div class="field", class: "label">
    <%= form.label :calories_lost, class: "label" %>
    <%= form.text_field :calories_lost, id: :post_user_id, class: "input" %>
  </div>
  
  <div class="field">
    <%= form.label :text, class: "label" %>
    <%= form.text_area :text, id: :post_user_id, class: "input" %>
  </div>

  <div class="actions">
    <%= form.submit "Create", class: "button is-primary is-rounded is-fullwidth" %>
  </div>
<% end %>
